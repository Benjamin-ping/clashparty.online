{
  "name": "clashparty-chinese-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test scripts/releases.test.mjs",
    "sync:releases": "node scripts/sync-releases.mjs",
    "deploy": "npm run build && npm run check && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
